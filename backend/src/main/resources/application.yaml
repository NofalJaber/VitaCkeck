spring:
  application:
    name: VitaCheck
  
  datasource:
    url: jdbc:postgresql://localhost:5432/${DB_NAME:vitadb}
    username: ${DB_USER:vita}
    password: ${DB_PASSWORD:vita.080120040}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update